#ifndef CALCULATIONS_HPP
#define CALCULATIONS_HPP
#include "basicLibraries.hpp"
#include "jsonValidation.hpp"
#include "json.hpp"
/* template <typename T>
class Calculations
{
private:
    std::string &m_dataPathFile;
    std::vector<T> &m_vectorDatabase;
    using json = nlohmann::json;
    json& m_JSONFile;

public:
    Calculations(std::string &path, std::vector<T> &properties) : m_dataPathFile{path}, m_vectorDatabase{properties} {};
    void validateJSON()
    {
    };
}; */
#endif